---
import TabButton from "@/components/TabButton.astro";
import { mathKeys } from "@/lib/keys";
---

<div class="bg-brands-light p-3">
  <div class="pointer-events-none flex gap-1 cursor-pointer">
    {mathKeys.map((key) => <TabButton key={key} />)}
  </div>
</div>
